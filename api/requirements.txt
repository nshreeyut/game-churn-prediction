# FastAPI backend dependencies
# These are likely already in your .venv.
# This file tells Render what to install when deploying the backend.

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Settings / validation
pydantic>=2.0
pydantic-settings>=2.0

# Data
polars>=1.0
joblib
numpy

# LangChain + LLM
langchain>=0.3.0
langchain-openai>=0.2.0
langchain-core>=0.3.0

# ML models (needed to deserialize the .joblib files saved during training)
scikit-learn
xgboost
lightgbm
catboost

# Explainability
shap
